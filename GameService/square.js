Square = function(x, y, width, height, col, row, isBattleFileldSquare = false){
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
    this.col = col,
    this.row = row;
    this.isBattleFileldSquare = isBattleFileldSquare;
};